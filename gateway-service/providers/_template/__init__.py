"""PROVIDER FACADE TEMPLATE — the official, stable reference (D2-bis).

This is NOT a working provider (see providers/_example/ for that). It is
the documented, line-by-line template of the ONE layer the platform sees.
A provider engineer opens adapter.py and learns everything the platform
requires — without reading any gateway core code.

The ``_`` prefix means auto-discovery SKIPS this package by design.
"""
