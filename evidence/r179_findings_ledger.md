# R179 findings ledger (every measurement that FAILED or contradicted the brief; a finding gets an entry, never an off-books fix)

Precedent: `evidence/r175_findings_ledger.md`. Severities: S1 release-blocking … S4 cosmetic.
Disposition of every row is mirrored in `docs/ai_orchestration_pack/R179_HANDOFF.md` §C.

| id | finding | evidence | severity | disposition |
|---|---|---|---|---|
| F-R179-01 | Durable profile: `POST /v1/execute` with any `conversation_id` answers constant 500. Root cause: `apps/composition/runtime.py` composes `InMemoryConversationStore()` for BOTH profiles while `executions.conversation_id` carries FK `fk_executions_conversation_id_conversations` (migration 0008) → `ForeignKeyViolationError` on the durable executions insert. Conversation continuity (4.4 P2) is therefore not merely non-durable — the conversational path is UNUSABLE on the durable profile. Discovered by the 4.4 probe on a real-alembic schema. | `evidence/r179/F01_execute_with_conversation_id_durable_500.txt`; probe `tests_live/r179/test_durability_measured_postgres.py` | S1 (durable-profile feature returns 500) | see HANDOFF §C  **Disposition (4.5, head 63625443): CLOSED — durable adapters bound; after-measure `evidence/r179/durability_measured.json`.** |
| F-R179-02 | Shipped runtime (`strict_promotion_evidence=True`, `apps/composition/runtime.py`): `POST /v1/admin/learning/samples/{id}/promote` can NEVER succeed over HTTP. `security_eval_pass` is artefact-backed and requires an evaluation record carrying a passing `GraderType.SECURITY` grader (`apps/api/promotion_evidence.py:149-158`), but SECURITY is outside `FINAL_ACTIVE_GRADER_TYPES` (`core/evaluation/graders.py:74`) and no production path emits it — so the condition is forced False and the gate refuses (governed mode → 409 "Learning operation refused."). GOLD is reachable only in-process (R178 live tests called `promote_to_gold` directly). Learning pillar "feedback → durable knowledge" is therefore INERT at the control plane. | `evidence/r179/durability_measured.json` (`arrange.p1_promote_status: 409`) | S2 (a shipped verb that cannot complete; policy-gated by design per L6 — reported, not bent) | see HANDOFF §C  **Disposition (rulings Q3, head 302fcb54): CLOSED BY CAPABILITY — `SecretMaterialGrader` (first real `GraderType.SECURITY`, 13 §7 credential scan, non-vacuous) activated through the designed `active_types` + `output_graders` path; live re-measure `evidence/r179/durability_measured_after_q3.json`: promote **201** with `offline/security/regression` all resolved from HTTP-produced records, `learned_keys ['durability.fact']` 1→1 across SIGKILL, `gold_blocks` 1→1, custody level GOLD; hermetic 409 for a credential-bearing sample in governed mode (`tests/api/test_security_grader_q3_r179.py`). The promote gate itself is unchanged.** |
| F-R179-03 | 4.4 P1 (knowledge injected into a later answer): the only HTTP-reachable memory writer (13 §6 preference learning, `PREFERENCE_LEARNING_ALLOWED=1`) produced 1 memory item that rode `before kill` as `memory_blocks=1`; after SIGKILL/restart `preference_items_after=[]` and `memory_blocks_after=0`. Cause: `apps/composition/runtime.py` composes `InMemoryMemoryStore()` on BOTH profiles although `DatabaseBindings.memory` (PostgresMemoryRepository over migration 0007) exists and is never bound. Injected context DIES WITH THE PROCESS → 4.5 condition TRIGGERED. GOLD arm not measurable over HTTP (F-R179-02). | `evidence/r179/durability_measured.json` P1 | S1 (vision line "knowledge measurably improves the next answer after a restart" is FALSE on the durable profile) | see HANDOFF §C  **Disposition (4.5, head 63625443): CLOSED — durable adapters bound; after-measure `evidence/r179/durability_measured.json`.** |
| F-R179-04 | 4.4 P3/P4 (audit, usage): audit `total_recorded` 1 after LOGIN in the boot process → 0 in the next process; usage `used` 5.0 before kill → 2.0 after restart (only the post-restart executions). `InMemoryAuditLog()` / `InMemoryUsageAccounting()` are composed for both profiles (`runtime.py:735-736`, comment "durable usage remains a later binding") while `DatabaseBindings.audit` / `.usage` repositories exist unbound. This is the recorded DEC-B deferral made visible by measurement; NOT repaired this round (B2/L6: honest deferral, docs must say so). | `evidence/r179/durability_measured.json` P3, P4 | S2 (billing/multi-app claims impossible until DEC-B) | see HANDOFF §C |
| F-R179-05 | 4.6-b rolling pair (live, `tests_live/r179/test_deploy_truth_rolling_crash_postgres.py`, old writer = worktree of commit 2c197229 = last pre-0020 commit): after `alembic upgrade head` (0020) the OLD binary boots on the 0020 database and `POST /v1/admin/learning/samples` answers **201** — it lands a custody row under the tenant-wide `legacy_unresolved` hold, while the NEW binary is correctly refused (404). The hold is enforced by the new repository code, not by the schema. Raw: `evidence/r179/deploy_truth_rolling_crash.json` → `rolling_pair`. | S2 | OPEN — mitigation is procedural only (OPERATIONS.md §8.1: stop ALL old writers before 0020; never resume old binaries). A schema-level guard (e.g. trigger/constraint refusing custody inserts while a hold row exists) needs a NEW migration — forbidden this round (L3) → decision queue. |
| F-R179-06 | Q1 live measurement (head be407d2e, `tests_live/r179/run_local_postgres.sh -k four_pillars`): binding `DurableUsageAccounting` in the durable profile makes EVERY `POST /v1/execute` answer 500 — `asyncpg.exceptions.ForeignKeyViolationError: insert or update on table "usage_ledger" violates foreign key constraint "fk_usage_ledger_execution_id_executions"`. Root cause is structural, not the adapter: `usage_ledger.execution_id` is `NOT NULL` + FK → `executions.id` (RESTRICT, UNIQUE; `infrastructure/db/tables.py:672-700`), but `ExecutionService.execute_*` mints `execution_id` and calls `usage.reserve()` (`core/execution/service.py:383,402`) BEFORE any executions row exists (03 §7 reserve-before-work), and `core/tools/executor.py:166` reserves under a `call_id` that never becomes an executions row at all. The in-memory ledger never saw this because it has no FK. DEC-B "usage 5.0→2.0 must become stable" is therefore blocked by schema/ordering, not by a missing seam. | `evidence/r179/F06_usage_ledger_fk_violation.txt` | S2 (durable usage cannot be bound without a contract change; billing claims still impossible) | OPEN → operator question Q6 (HANDOFF §B): (a) reserve AFTER the executions row (changes the 03 §7 refusal-as-data ordering and leaves tool calls unkeyed), or (b) new migration relaxing the FK / adding a ledger key that is not an executions id. Neither is in the rulings' scope. Runtime: audit bound durably (fix commit after be407d2e); durable usage adapter composed, then consciously NOT bound (`del durable_usage`, one-name flip) — usage stays process-local in BOTH profiles, stated in OPERATIONS §13. Audit/usage screens still do not ship (Q1 ruling). |
| F-R179-07 | Q1 binding: the FROZEN `apps/admin_agent/tools.py:84` annotates `AgentToolSurface.usage: InMemoryUsageAccounting` (concrete class) while the only use is `surface.usage.summary(...)` (line 185). Binding the durable usage adapter trips mypy at the composition call site. | S4 | OPEN — one `# type: ignore[arg-type]` at the runtime call site with the structural fact pinned by `tests/composition/test_durable_audit_usage_r179.py::test_frozen_agent_tool_surface_uses_only_summary_on_usage`; the annotation is corrected in the Q5 thaw commit (next round). No frozen tree edited. |
