# R179 findings ledger (every measurement that FAILED or contradicted the brief; a finding gets an entry, never an off-books fix)

Precedent: `evidence/r175_findings_ledger.md`. Severities: S1 release-blocking … S4 cosmetic.
Disposition of every row is mirrored in `docs/ai_orchestration_pack/R179_HANDOFF.md` §C.

| id | finding | evidence | severity | disposition |
|---|---|---|---|---|
| F-R179-01 | Durable profile: `POST /v1/execute` with any `conversation_id` answers constant 500. Root cause: `apps/composition/runtime.py` composes `InMemoryConversationStore()` for BOTH profiles while `executions.conversation_id` carries FK `fk_executions_conversation_id_conversations` (migration 0008) → `ForeignKeyViolationError` on the durable executions insert. Conversation continuity (4.4 P2) is therefore not merely non-durable — the conversational path is UNUSABLE on the durable profile. Discovered by the 4.4 probe on a real-alembic schema. | `evidence/r179/F01_execute_with_conversation_id_durable_500.txt`; probe `tests_live/r179/test_durability_measured_postgres.py` | S1 (durable-profile feature returns 500) | see HANDOFF §C  **Disposition (4.5, head 63625443): CLOSED — durable adapters bound; after-measure `evidence/r179/durability_measured.json`.** |
| F-R179-02 | Shipped runtime (`strict_promotion_evidence=True`, `apps/composition/runtime.py`): `POST /v1/admin/learning/samples/{id}/promote` can NEVER succeed over HTTP. `security_eval_pass` is artefact-backed and requires an evaluation record carrying a passing `GraderType.SECURITY` grader (`apps/api/promotion_evidence.py:149-158`), but SECURITY is outside `FINAL_ACTIVE_GRADER_TYPES` (`core/evaluation/graders.py:74`) and no production path emits it — so the condition is forced False and the gate refuses (governed mode → 409 "Learning operation refused."). GOLD is reachable only in-process (R178 live tests called `promote_to_gold` directly). Learning pillar "feedback → durable knowledge" is therefore INERT at the control plane. | `evidence/r179/durability_measured.json` (`arrange.p1_promote_status: 409`) | S2 (a shipped verb that cannot complete; policy-gated by design per L6 — reported, not bent) | see HANDOFF §C |
| F-R179-03 | 4.4 P1 (knowledge injected into a later answer): the only HTTP-reachable memory writer (13 §6 preference learning, `PREFERENCE_LEARNING_ALLOWED=1`) produced 1 memory item that rode `before kill` as `memory_blocks=1`; after SIGKILL/restart `preference_items_after=[]` and `memory_blocks_after=0`. Cause: `apps/composition/runtime.py` composes `InMemoryMemoryStore()` on BOTH profiles although `DatabaseBindings.memory` (PostgresMemoryRepository over migration 0007) exists and is never bound. Injected context DIES WITH THE PROCESS → 4.5 condition TRIGGERED. GOLD arm not measurable over HTTP (F-R179-02). | `evidence/r179/durability_measured.json` P1 | S1 (vision line "knowledge measurably improves the next answer after a restart" is FALSE on the durable profile) | see HANDOFF §C  **Disposition (4.5, head 63625443): CLOSED — durable adapters bound; after-measure `evidence/r179/durability_measured.json`.** |
| F-R179-04 | 4.4 P3/P4 (audit, usage): audit `total_recorded` 1 after LOGIN in the boot process → 0 in the next process; usage `used` 5.0 before kill → 2.0 after restart (only the post-restart executions). `InMemoryAuditLog()` / `InMemoryUsageAccounting()` are composed for both profiles (`runtime.py:735-736`, comment "durable usage remains a later binding") while `DatabaseBindings.audit` / `.usage` repositories exist unbound. This is the recorded DEC-B deferral made visible by measurement; NOT repaired this round (B2/L6: honest deferral, docs must say so). | `evidence/r179/durability_measured.json` P3, P4 | S2 (billing/multi-app claims impossible until DEC-B) | see HANDOFF §C |
