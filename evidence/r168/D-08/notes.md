# D-08 — `project_id` on `/v1/execute` silently accepted and ignored (S3, SILENT)

- FAIL FIRST: `tests/api/test_d08_execute_project_reference.py` — two tenants over ONE `InMemoryProjectStore`; foreign pid / unknown UUID / `not-a-uuid` on `POST /v1/execute` answered 503 (ran on into composition with no adapters) instead of the projects surface's 404 — `fail_first.txt` (3 failed / 1 passed; the owned-project case already passed by accident because the field was ignored).
- FIX (Round A change #3, `apps/api/app.py`, +22/-1): the project store is hoisted to ONE `project_store` used by both `create_workspace_router(...)` and `/v1/execute`; in `execute`, after identity and rate limit and BEFORE scope rejections / idempotency replay / persistence: `UUID(body.project_id)` failure ⇒ `unknown_project(ref)`; `await project_store.get(caller.tenant_id, ref)` raising `ProjectNotFound` (absent == foreign, 20 §6) ⇒ `unknown_project(ref)`. A bad reference leaves zero state.
- FIX (Round A change #4, `apps/api/workspaces.py`, +8/-1): `_unknown_project` promoted to the exported `unknown_project` (alias `_unknown_project` kept for the module's own routes) so both surfaces share the SAME 404 body: `{"error":{"code":"validation_error","message":"Unknown project id.","details":{"project_id":"<as given>"}}}`, HTTP 404.
- Decision: malformed (`not-a-uuid`) on `/v1/execute` is treated as UNKNOWN (404), not as a 422 field error — a non-UUID can never name a project, and distinguishing it would be a shape oracle on a body field. (`GET /v1/projects/{not-a-uuid}` keeps its existing 422 path-parameter contract; unchanged.) Attachment of the execution to the project record is NOT introduced (no consumer of the reference downstream; that is a feature, not this defect).
- INV-2: the mandate explicitly permits the `project_id` semantics change ("except D-01 settlement and D-08 project_id"). Field kept in `ExecuteRequest`.
- PASS: `after_fix.txt` — 4 passed. Slices `tests/api tests/composition`: 602 passed / 4 skipped. mypy clean (181 files), ruff clean.
