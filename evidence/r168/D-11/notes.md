# D-11 — audit coverage: denials left no rows (S3, AUDIT-GAP)

- Defect: `PERMISSION_DENIED`, `CROSS_TENANT_ACCESS_DENIED`, `CREDENTIAL_CREATED/REVOKED`, `PROVIDER_ACCOUNT_USED` had zero emitters; `GET /v1/admin/audit` after 30+ denials listed `['login']` only; the read is reader-tenant-scoped.
- FAIL FIRST (ca2bba0b): `tests/composition/test_d11_denials_are_audited.py` over `build_runtime_profile` (auth + admin composed; the runtime's ONE `InMemoryAuditLog` captured by monkeypatching the class in the runtime module). 4 failed — HTTP 403/404 bodies already correct, zero rows written (`fail_first.txt`).
- FIX (Round B change #4, `apps/api/app.py`, +37/−0): `_audit_denied(request, caller, event_type, details)` over `admin.audit` (absent ⇒ no-op). Emitted at (a) the admission middleware's non-admin `/v1/admin/*` 403 → `PERMISSION_DENIED {method, path, reason: admin_required}`; (b) the D-08 `project_id` admission on `/v1/execute` for malformed AND unresolved references → `CROSS_TENANT_ACCESS_DENIED {method, path, resource: project, reference}`. Row tenant = ACTOR's tenant, `actor_id` = caller. No request body, no secret, no target-tenant fact in details; response bodies unchanged (asserted byte-identical to the projects surface 404).
- Design (INV-4): foreign/unknown/malformed are recorded identically — the gate cannot distinguish them (20 §6) and the audit row must not know more than the response. Anonymous 401 not audited (no principal → no tenant). Owner tenant receives nothing (would be an oracle).
- PASS (02e8a6e1): 4 passed (`after_fix.txt`); `tests/api tests/composition tests/verification` 625 passed / 4 skipped; ruff + `mypy --strict apps/api/app.py` clean.
- NOT FIXED in R168 (verbatim): `CREDENTIAL_CREATED/REVOKED` — no credential lifecycle HTTP route exists to emit from; `PROVIDER_ACCOUNT_USED` at composition — depends on D-03/D-04 runtime wiring (R169); PLATFORM-wide audit read across tenants — `AuditLogPort` forbids cross-tenant reads by design and no platform-admin identity exists: OUT OF SCOPE (R168): design — scheduled for R169.
- Budget: Round B 4/5 (`changes_used == len(log)`; item "D-11" scheduled). D-02 remains for the last slot only if `providers/`-local (budget-free) — evaluated next.
- Docs: `evidence/defect_ledger.md` D-11 FIXED (partial) + summary; IMPL-011 in `60_DECISION_LOG.md`.
