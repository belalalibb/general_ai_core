# D-07 — token-less caller silently mapped to the demo principal (S2, POSTURE)

- FAIL FIRST: `tests/composition/test_d07_tokenless_401.py` — ImportError (no `DEV_DEMO_PRINCIPAL_ENV`/`PUBLIC_PATHS`), then after the opt-in change alone the route sweep still leaked 22 operations (422 before identity on typed POSTs; `GET /v1/models`, `GET /v1/skills` 200; `POST /v1/auth/logout` 204) — `fail_first.txt`.
- FIX (Round A change #1, `apps/composition/runtime.py`, +24/-1): `DEV_DEMO_PRINCIPAL_ENV = "DEV_DEMO_PRINCIPAL"` — the demo principal is composed ONLY when the literal `"1"` is set, in-memory profile only (durable ignores it); `PUBLIC_PATHS` frozenset = `/healthz`, `/v1/auth/{register,verify,login,logout}`; passed as `create_app(public_paths=PUBLIC_PATHS)`.
- FIX (Round A change #2, `apps/api/app.py`, +50/-1, shared with D-10): `create_app(public_paths=…)` + one `@app.middleware("http")` registered only when `auth` is composed: path in public set or outside `/v1/` ⇒ pass; `/v1/admin/*` without a bearer ⇒ the constant 401; otherwise `_principal(request)` (hybrid fallback honoured) ⇒ 401 on missing/invalid bearer; `/v1/admin/*` non-admin ⇒ 403; only then does FastAPI validate the body. No route signature changed. `_AUTH_ENTRY_PATHS` (register/verify/login/logout) are public by construction — a composition can add public paths, never remove these.
- Decision on `/v1/auth/logout`: kept public because its frozen contract is "ALWAYS 204, never a token-validity oracle" (`apps/api/auth.py`); a 401 for a missing bearer would be that oracle. Not tenant-bearing for an unadmitted caller. `/v1/auth/session` stays identity-bearing (401 without a token).
- PASS: `after_fix.txt` — 5 passed (default profile has no demo principal; all 79 served paths minus public ⇒ 401 `unauthenticated`; public list exact; `DEV_DEMO_PRINCIPAL=1` restores the demo principal, never admin; only the literal "1" opens it).
- Tests moved to the explicit opt-in where they exercise the demo tenant (`DEV_DEMO_PRINCIPAL: "1"` in `environ`): test_admin_console_runtime, test_agent_composition_r160, test_gold_reaches_production_r161, test_ui_app_pd2, test_runtime_pb, test_handoff_gaps. `test_cli_entrypoint` asserts `demo_principal is False` by default and `True` under the opt-in. `test_provider_onboarding_hydration::TestRuntimeSeam` proves route presence via served OpenAPI and asserts the constant 401 on the wire for absent AND present (anti-enumeration).
- Docs: OPERATIONS §0 (zero-config posture = auth only; opt-in command), §3 (identity-modes table, public paths, middleware order), troubleshooting rows for 401/403 — same commit. Decision log IMPL-007.
