# R175 state ledger (checkpoint discipline: one row appended BEFORE each item; recoverable from this file alone)

Contract: R175 REV 4 — FINAL BACKEND CERTIFICATION / REAL-PROVIDER HARD GATE, governed by R168 (REV 3). Additive only.
Deliverables: `evidence/r175/**` (raw), `evidence/r175_findings_ledger.md` (machine-readable), `docs/r175/R175_FINAL_BACKEND_CERTIFICATION.md` (human report).
Verdict is COMPUTED from the findings ledger (§29). Secrets: env-only ingress, never in evidence.

| item | intended change / result | HEAD at start |
|---|---|---|
| §0 | Recovery: fresh sandbox, HEAD==origin/main 45e0eb7, tree clean, venv absent → rebuilt (`pip install -e ".[dev]"` + gateway-service reqs). No engineering state lost (R174 closed at 45e0eb7). | 45e0eb7 |
| §5-A | BASELINE (before any edit): `evidence/r175/00_baseline/{git_state.txt,check_repo_baseline.txt,ruff_baseline.txt}`. Hermetic `python3 -m apps.cli check` → pytest 3150/0/0/64 (floor 3127, max_skipped 64), mypy clean, import-linter kept, secret scan 5/5, budgets within ceilings, not_evaluated=2 — **ruff FAIL → RESULT: FAIL, EXIT=1**. 10× E501 in 4 files, all introduced in R174 (b131345, 59077a0, 76e7b87), which never ran check_repo. Finding F-R175-01 (BLOCKER: the only green gate is red). | 45e0eb7 |
| §0-b | **Sandbox reset #12** (between turns, mid-§2): venv, Postgres 17, pgvector all lost; repo + committed evidence intact (HEAD d89de3e, tree clean). Rebuilt: venv + `pip install -e ".[dev]"`, apt `postgresql postgresql-contrib postgresql-17-pgvector`, role `qevion`, db `qevion`, `CREATE EXTENSION vector`, `alembic upgrade head` → `0018`. A background pytest run started before the reset produced NO evidence file — rerun in foreground. | d89de3e |
| §2-A | DURABLE PROFILE (real Postgres, `DATABASE_URL` set): `evidence/r175/02_durable_profile/pytest_with_database_url.txt` — **3197 passed / 17 skipped / 1 xfailed / 0 failed** in 67 s. Skips 64 → 17: all 41 DB-gated tests now execute and pass (memory_embeddings/pgvector included). Remaining 17 skips are credential-only: GROQ_API_KEY ×6, GW_GROQ_API_KEY ×1, VAULT ×4, OBJECT_STORAGE ×4, GSK plan-exhausted ×2. xfail = `test_denied_paths_r172` obfuscated-name case (by design). 1 warning = Starlette httpx deprecation (upstream, not ours). | d89de3e |
| §2-A′ | PUSH BLOCKED: GitHub credentials absent after reset #12 (`setup_github_environment` → no token). a5a4cbc (§2 evidence) bundled → https://www.genspark.ai/api/files/s/lL5UCBbc (restore on a fresh clone at d89de3e: `git fetch <bundle> main && git merge --ff-only FETCH_HEAD`). Next: §3 real-provider gate — needs GROQ_API_KEY / GW_GROQ_API_KEY in the driver env (currently unset; the earlier inline-key probe was interrupted and is NOT counted — no evidence, no call recorded). | a5a4cbc |
| §0-c | **Sandbox reset #13** (between turns): venv, Postgres, bundle file, GitHub creds all lost again; repo at d89de3e. Restored d89de3e..ce37a98 byte-identical from bundle BjIUjKL6 (ff, verified). GitHub token now persisted in `~/.git-credentials` (mode 600, outside the tree, `credential.helper store`) → pushed, origin/main == ce37a98. Rebuilt venv + Postgres 17 + pgvector, `alembic upgrade head` → 0018. Exact-token grep across tracked files = 0 hits (the `ghp_` matches are the sanitizer/pre-commit regexes). | ce37a98 |
| §3-A | REAL-PROVIDER HARD GATE — driver `evidence/r175/03_real_provider_gate/run_gate.py`. Rule (user-set): the 7 Groq-gated tests (`test_groq_live.py` ×5, `test_groq_live_e2e.py` ×1, `test_gateway_groq_live_e2e.py` ×1) are RAW until a real upstream call to api.groq.com succeeds; promotion to `evidence/` happens ONLY when pytest reports 7 passed / 0 skipped / 0 failed, otherwise the run lands in `raw/` marked NOT_PROMOTED. Key ingress: env only (`GROQ_API_KEY` + `GW_GROQ_API_KEY`, sourced from a mode-600 file outside the repo); driver scrubs the literal key + `gsk_[A-Za-z0-9]{20,}` shape and asserts absence before writing. | ce37a98 |
| §0-d | **Sandbox reset #14** (between turns): venv + Postgres gone; repo intact at 3dd8dd5 with the uncommitted two-key driver edit surviving. Rebuilt venv, Postgres 17 + pgvector, `alembic upgrade head` → 0018. | 3dd8dd5 |
| §3-B | Driver generalised to two independent keys (`GROQ_API_KEY` for the platform adapter, `GW_GROQ_API_KEY` for the gateway Layer-1; each probed separately, promotion requires every probe OK). No-key dry run re-verified (`raw_run0_nokey/`: probe SKIPPED, 7 skipped, PROMOTED=false, exit 1). **LIVE RUN** (`raw/`, keys as process env only): both probes → HTTP 400 `organization_restricted` ("Organization has been restricted"); pytest **6 failed / 1 passed / 0 skipped**; **PROMOTED=false**. The 7 Groq tests carry NO certification weight. Attribution: upstream ACCOUNT state (Groq-side), not platform. Paid calls: 0 (every generation rejected). Only pass = `test_live_key_never_in_response_artifacts` (key-hygiene, holds regardless of upstream). | 3dd8dd5 |
| §3-C | FINDING F-R175-02 surfaced by the live run: platform adapter maps `organization_restricted` → `invalid_credential` (route-indicting, failover allowed; fixed 2026-09-03), but gateway `providers/groq/adapter.py` maps the same code → `bad_request` (failover forbidden) via the bare status table — despite its docstring claiming to mirror the platform mapping. Live proof: `/v1/execute` via platform adapter → `provider_error_category=invalid_credential`; via gateway → `bad_request` for the identical upstream reply. Repair (gateway-service only, zero core/ lines): account-indicting code set checked before the status table; test-pinned. | 3dd8dd5 |
| §4 | F-R175-02 REPAIRED (64776a8): gateway `providers/groq/adapter.py` gains `_ACCOUNT_INDICTING_CODES = {organization_restricted}` checked before the status table → `invalid_credential` (retryable=false, provider_code kept, fixed safe message). Pinned by 2 tests (category + AST-parity with the platform set, read by path, no cross-project import). Both fail on the pre-fix adapter (stash check). Gateway suite 194/194, ruff clean. core/ apps/ lines changed = 0. Live re-run (`not_promoted/run2_post_f02/`): still 6F/1P (upstream unchanged), but the gateway entry point now reports `invalid_credential` — run1 had `bad_request` for the identical upstream reply. Still NOT_PROMOTED: repair fixes classification, not the blocked Groq account. Housekeeping: `raw_*` is gitignored (R172 §0), so run0 was never tracked — all NOT_PROMOTED runs moved under `not_promoted/`; driver dest updated. | b283f0f |
| §5-B | FINAL GATES after repairs: hermetic `apps.cli check` → **RESULT PASS, EXIT 0** (pytest 3156/0/0/58, mypy, ruff, import-linter, secret scan, budgets all green; not_evaluated=2 verbatim). Durable profile re-run → 3197/0/17/1xfail, unchanged. First §5 attempt ran with bare `python3` (no venv on PATH) → 86 collection errors/FAIL — recorded as F-R175-04 (ops footgun), superseded by the venv run. `evidence/r175/05_final_check/`. | b583b3c |
| §6 | DELIVERABLES: `evidence/r175_findings_ledger.md` (machine-readable; verdict computed §E) + `docs/r175/R175_FINAL_BACKEND_CERTIFICATION.md`. **VERDICT: BACKEND CERTIFIED — HERMETIC + DURABLE. REAL-PROVIDER GATE: NOT SATISFIED (external F-R175-03, Groq org restricted on both keys).** Partial certification; UI NOT authorised by this round. Push blocked (creds wiped by reset #14) → bundle of 3dd8dd5..HEAD uploaded (see next row). | b583b3c |
| §6′ | PUSH BLOCKED (GitHub creds wiped by reset #14; `setup_github_environment` → none). 3dd8dd5..733c188 (9 commits) bundled → https://www.genspark.ai/api/files/s/bYFLzDGr (restore on a clone at 3dd8dd5: `git fetch <bundle> HEAD && git merge --ff-only FETCH_HEAD`). Interim bundle DS5UlNxJ covers 3dd8dd5..b583b3c. Final tracked-tree scan for `gsk_[A-Za-z0-9]{20,}` = 0 hits. R175 deliverables complete; round closes at the verdict above. | 733c188 |
| §0-d | **Sandbox reset #15** (between turns): repo reverted to 3dd8dd5; venv/Postgres lost. Restored 3dd8dd5..2e384fc byte-identical from bundle aLogFeKb (ff, verified). GitHub token supplied in handoff → credential store (never written to repo). `git fetch origin main`: origin still at 3dd8dd5, 0 behind → clean ff push of the 7 R175 commits + this ledger entry to main. Bundles mhIDDbA5 / aLogFeKb remain as off-sandbox copies. No verified or paid work re-run. | (this commit) |
| §0-e | **Sandbox reset #16** (between turns): venv/Postgres/creds lost; repo intact at c925ca5 == origin/main (tree clean, 0/0 divergence after fetch). Token from handoff → `~/.git-credentials` (600, outside tree). Venv rebuilt; hermetic `apps.cli check` re-run on the resumed sandbox → **RESULT PASS, EXIT 0** (3156/0/0/58, mypy/ruff/import-linter/secret-scan/budgets green, not_evaluated=2) — matches §5-B verbatim. R175 remains CLOSED at its recorded verdict; only open items are F-R175-03 (external: unrestricted Groq key) and F-R175-04 (S3 ops). No new engineering work started; awaiting an unrestricted provider key or an explicit R176 contract. | c925ca5 |
| §3-D | **REAL-PROVIDER HARD GATE — PROMOTED (run3).** Three Groq keys supplied in the handoff; tested one at a time, process-env only, driver `run_gate.py` UNCHANGED (verified `git status` clean before run). Key #1 (as both `GROQ_API_KEY` and `GW_GROQ_API_KEY`): probe `GET /openai/v1/models` → HTTP 200 ×2 (14 models, allam-2-7b present, 170 ms); pytest **7 passed / 0 skipped / 0 failed / 0 errors** in 3.83 s → `PROMOTED=true`, EXIT 0, artifacts written to `evidence/r175/03_real_provider_gate/{verdict.json,pytest_live_report.txt}` (scrubbed; `gsk_` shape 0 hits across evidence+docs). Keys #2/#3 NOT used (rule: first key that actually satisfies the gate; no extra paid calls). Paid calls upper bound: 4. F-R175-03 → CLOSED (external blocker lifted by a working account, not by code). Next: fold L-03 into findings ledger + report, recompute verdict. | e08c5798 |
| §6-B | Findings ledger: L-03 added as EVIDENCE; F-R175-03 → CLOSED (external cause removed, 0 code change); verdict recomputed `real_provider_gate=TRUE` → **BACKEND CERTIFIED — HERMETIC + DURABLE + REAL-PROVIDER GATE SATISFIED**. Human report §1/§2/§3/§5/§6/§7 updated; superseded verdict kept inline for the record. §6 answers the UI question from repository reality: no `U-16`/`U-17` identifiers exist in the tree; `ui/app` primary capability = `POST /v1/execute` → proven live by L-03; `ui/admin` routes covered by hermetic+durable suites + prior 32/32 browser proof. UI not started/modified. Tree secret scan: only the 3 declared test-fixture `gsk_` strings. | d8be61b3 |
