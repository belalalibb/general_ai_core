# R175 state ledger (checkpoint discipline: one row appended BEFORE each item; recoverable from this file alone)

Contract: R175 REV 4 — FINAL BACKEND CERTIFICATION / REAL-PROVIDER HARD GATE, governed by R168 (REV 3). Additive only.
Deliverables: `evidence/r175/**` (raw), `evidence/r175_findings_ledger.md` (machine-readable), `docs/r175/R175_FINAL_BACKEND_CERTIFICATION.md` (human report).
Verdict is COMPUTED from the findings ledger (§29). Secrets: env-only ingress, never in evidence.

| item | intended change / result | HEAD at start |
|---|---|---|
| §0 | Recovery: fresh sandbox, HEAD==origin/main 45e0eb7, tree clean, venv absent → rebuilt (`pip install -e ".[dev]"` + gateway-service reqs). No engineering state lost (R174 closed at 45e0eb7). | 45e0eb7 |
| §5-A | BASELINE (before any edit): `evidence/r175/00_baseline/{git_state.txt,check_repo_baseline.txt,ruff_baseline.txt}`. Hermetic `python3 -m apps.cli check` → pytest 3150/0/0/64 (floor 3127, max_skipped 64), mypy clean, import-linter kept, secret scan 5/5, budgets within ceilings, not_evaluated=2 — **ruff FAIL → RESULT: FAIL, EXIT=1**. 10× E501 in 4 files, all introduced in R174 (b131345, 59077a0, 76e7b87), which never ran check_repo. Finding F-R175-01 (BLOCKER: the only green gate is red). | 45e0eb7 |
| §0-b | **Sandbox reset #12** (between turns, mid-§2): venv, Postgres 17, pgvector all lost; repo + committed evidence intact (HEAD d89de3e, tree clean). Rebuilt: venv + `pip install -e ".[dev]"`, apt `postgresql postgresql-contrib postgresql-17-pgvector`, role `qevion`, db `qevion`, `CREATE EXTENSION vector`, `alembic upgrade head` → `0018`. A background pytest run started before the reset produced NO evidence file — rerun in foreground. | d89de3e |
| §2-A | DURABLE PROFILE (real Postgres, `DATABASE_URL` set): `evidence/r175/02_durable_profile/pytest_with_database_url.txt` — **3197 passed / 17 skipped / 1 xfailed / 0 failed** in 67 s. Skips 64 → 17: all 41 DB-gated tests now execute and pass (memory_embeddings/pgvector included). Remaining 17 skips are credential-only: GROQ_API_KEY ×6, GW_GROQ_API_KEY ×1, VAULT ×4, OBJECT_STORAGE ×4, GSK plan-exhausted ×2. xfail = `test_denied_paths_r172` obfuscated-name case (by design). 1 warning = Starlette httpx deprecation (upstream, not ours). | d89de3e |
| §2-A′ | PUSH BLOCKED: GitHub credentials absent after reset #12 (`setup_github_environment` → no token). a5a4cbc (§2 evidence) bundled → https://www.genspark.ai/api/files/s/lL5UCBbc (restore on a fresh clone at d89de3e: `git fetch <bundle> main && git merge --ff-only FETCH_HEAD`). Next: §3 real-provider gate — needs GROQ_API_KEY / GW_GROQ_API_KEY in the driver env (currently unset; the earlier inline-key probe was interrupted and is NOT counted — no evidence, no call recorded). | a5a4cbc |
| §0-c | **Sandbox reset #13** (between turns): venv, Postgres, bundle file, GitHub creds all lost again; repo at d89de3e. Restored d89de3e..ce37a98 byte-identical from bundle BjIUjKL6 (ff, verified). GitHub token now persisted in `~/.git-credentials` (mode 600, outside the tree, `credential.helper store`) → pushed, origin/main == ce37a98. Rebuilt venv + Postgres 17 + pgvector, `alembic upgrade head` → 0018. Exact-token grep across tracked files = 0 hits (the `ghp_` matches are the sanitizer/pre-commit regexes). | ce37a98 |
| §3-A | REAL-PROVIDER HARD GATE — driver `evidence/r175/03_real_provider_gate/run_gate.py`. Rule (user-set): the 7 Groq-gated tests (`test_groq_live.py` ×5, `test_groq_live_e2e.py` ×1, `test_gateway_groq_live_e2e.py` ×1) are RAW until a real upstream call to api.groq.com succeeds; promotion to `evidence/` happens ONLY when pytest reports 7 passed / 0 skipped / 0 failed, otherwise the run lands in `raw/` marked NOT_PROMOTED. Key ingress: env only (`GROQ_API_KEY` + `GW_GROQ_API_KEY`, sourced from a mode-600 file outside the repo); driver scrubs the literal key + `gsk_[A-Za-z0-9]{20,}` shape and asserts absence before writing. | ce37a98 |
