# CS1 — CURRENT-STATE SECURITY + QUALITY + LIVE VALIDATION (new operator task, 2026-09-16)

**This is a NEW validation task. It is NOT a continuation of R186.** R186 is CLOSED (`r186-close-records` → `a99e2545`) and its records are not modified by this task. Start point: `main` = `a99e254520a693099b2e39b7cdb1e6f5c6c0f610`. Work branch `genspark_ai_developer_cs1` (records + any governed fix). Authority: operator task text ("Your new mission is CURRENT-STATE SECURITY + QUALITY + LIVE VALIDATION … Do NOT resume R186").

Order mandated: SECURITY/CORRECTNESS AUDIT → ROOT CAUSE → AUTONOMOUS FIXES (governed) → REAL VERIFICATION → REAL PREVIEW → REAL BROWSER E2E → REAL ASSEMBLYAI VALIDATION IF SUPPORTED → FINAL GAP REPORT.

Secrets: AssemblyAI key and GitHub credential are used only through process environment / transient push URL; never written to any file in this repository. Every evidence file is scanned (admin password, 32-hex key shapes, `ghp_`) before commit. Drivers live outside the repo (`/home/user/preview/`, lost on sandbox reset; re-created identically).

| # | step | measurement / command | result | evidence / status |
|---|---|---|---|---|
| 1 | Phase 0 — reconcile | `main` a99e2545 == origin/main; tree clean; tags r185-ui-live-freeze/r186-close/r186-close-records intact; 0 open PRs; 5 preserved branches; env: no GROQ_API_KEY (sandbox reset), GSK_*/OPENAI_*/ANTHROPIC_* present (stripped from every preview process); serving = `python3 -m apps.main` only (no deploy config); Command Center = `ui/app/command/`; providers/real = gateway, genspark_llm, groq | AssemblyAI: NOT a direct platform provider; IS a gateway-service provider (`gateway-service/providers/assemblyai`, `generate_text`, models qwen3.5-4b-32k-fast / gemini-2.5-flash-lite, `health_supported: False`); reachable from the platform only through the gateway binding (`GATEWAY_BASE_URL/SECRET/SECRET_VERSION/ROUTE_TOKENS`) + admin onboarding (`POST /v1/admin/providers/onboard`, refs only) + change-lifecycle enable — proven live once in R174 §7 (`evidence/r174/07_live_chain/run3_paid/X2_execute_assemblyai_paid.json`: 200 succeeded) | this ledger |
| 2 | Sandbox reset #1 during Phase 1 | lost: `.venv`, `/home/user/preview/*`, unpushed CS1 commit 3098a966, `evidence/cs1/api_probe.json` (first run: 38 VERIFIED / 2 FAILED — trace route 404 on `/v1/executions/{id}/trace`, unknown model → 503 `model_unavailable`; REAL AssemblyAI execute 200 `succeeded` in 539 ms, content "chains\nconfirmed") | re-created from records; every step now pushed immediately; the first-run numbers above are NOT evidence until re-measured in row 3+ | — |
| 3 | Phase 1 — live security/correctness audit over HTTP (platform 8080 + gateway 8800, both processes stripped of every ambient provider var; AssemblyAI key ONLY in the gateway env as `GW_ASSEMBLYAI_API_KEY`) | `/home/user/preview/api_probe.py` (outside repo) → **58 VERIFIED / 0 FAILED / 0 BLOCKED**. Auth: register 201 → console token verify 200 → login 200 opaque token; wrong pw / unknown email / garbage bearer / wrong scheme → identical constant 401 (no enumeration, no scheme oracle); duplicate register → generic 422; logout 204 → session 401; logout of revoked token 204 (no liveness oracle). Authz/tenancy: anonymous admin 401; non-admin on 6 admin routes 403; cross-tenant execution read/events 404 (anti-enumeration), admin evaluations 403; list isolation; `tenant_id` query cannot re-scope. API: malformed JSON / unknown field / oversized ask / non-uuid → 422 envelopes, no traceback; path-like `model_id` → 503 `model_unavailable`; `/docs` 404; no CORS allow for foreign origin; static traversal 404; unknown route 404. Provider boundary: `/v1/admin/system` names only; disabled provider → 503 (no upstream call); unknown model → 503; unresolvable `route_token_ref` → 409 refusal (R174 F-3 fixed holds); gateway without/with wrong secret → 401 | **Findings (informational, not defects of contract):** (I-1) `/openapi.json` served unauthenticated (75 KB route inventory; no secrets) while `/docs` is off; (I-2) no `X-Content-Type-Options` / `X-Frame-Options` / CSP / HSTS headers on the served UI; (I-3) `POST /v1/admin/changes` for an unknown provider returns 201 draft and is refused at `validate` (state → `rejected`, later steps 409) — correct lifecycle, recorded for clarity. No 5xx, no stack leak, no cross-tenant leak observed | `evidence/cs1/api_probe.json` |
| 4 | Phase 6 — AssemblyAI REAL validation through the platform's real admin door (support boundary proven first: gateway-service provider only) | onboard `assemblyai` refs-only → 201 registered DISABLED (`steps_passed` 3/4/11/13/12); enable via change lifecycle draft→validate→preview→publish all 200; `/v1/models` lists `assemblyai/qwen3.5-4b-32k-fast`; `POST /v1/execute explicit_model` → **200 `succeeded` in 462 ms, content exactly "chain verified"**, usage reserved 1 / settled 1; record 200; usage ledger row settled; `evaluation_status` = `NEVER_EVALUATED` on the usage row; `/v1/admin/executions/{id}/evaluations` 200 `[]`; audit readable | **AssemblyAI = SUPPORTED (gateway path) and VERIFIED live** — capability `generate_text` (LLM gateway, text generation; NOT speech). Paid upstream calls this task: 1. Key absent from gateway.log / platform.log / evidence (grep = 0) | `evidence/cs1/api_probe.json` rows "onboard…", "REAL execute…" |
