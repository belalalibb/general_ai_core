# R182-IMPL findings ledger (every measurement that FAILED or contradicted the brief; a finding gets an entry, never an off-books fix)

Precedent: `evidence/r182_findings_ledger.md`. Round budget: `round_r182` ceiling **0** — no finding may be fixed inside `core/ apps/ infrastructure/`.

| id | finding | evidence | severity | disposition | owning round |
|---|---|---|---|---|---|
| F-R182I-01 | `#center-view` carried `display:grid` in a class rule, which beat the HTML `hidden` attribute — the login panel and the (empty) center were both visible before sign-in. Found ONLY by the real-browser proof (text-level tests cannot see cascade order). | first browser run on `f41d6ee8` (`assert page.locator("#center-view").is_hidden()` failed) | S3 (honesty: a surface shown before its data exists) | FIXED in `ui/app/command/command.css` (`[hidden]{display:none!important}`) — UI tree, zero budget | R182-IMPL (closed) |
| F-R182I-02 | Fresh-clone gate on `26b98684` → **FAIL**: `skipped=65 (ceiling 64)`. The browser proof still had a `pytest.skip` path for "chromium not launchable", and the `env -i … HOME=/tmp` gate could not see the browser under `~/.cache/ms-playwright`, so it skipped — i.e. the removed NOT-EVALUATED #1 came back disguised as a skip. Also libatk/libatspi/libXcomposite were absent on a bare host (first `chromium.launch` died). | gate transcript on `26b98684` (superseded; gate of record = `evidence/r182_impl/gate_head_534649e8.txt`) | S2 (gate integrity; a skip is not an evaluation) | FIXED in tests/docs/manifest only: skip path REMOVED (missing browser now FAILS with the install command); browser installed INTO the venv (`PLAYWRIGHT_BROWSERS_PATH=0`) and the module selects that store when the variable is unset, so the stripped-env gate runs it; system libs via `playwright install-deps chromium`; `max_skipped` 64 NOT raised; no NE line added; `live-suite` manifest item re-described as EVALUATED. | R182-IMPL (closed) |
| F-R182I-03 | Browser store location is host-specific: the gate's `env -i` posture makes a browser under `$HOME/.cache` invisible; every fresh clone that wants the gate green must run `PLAYWRIGHT_BROWSERS_PATH=0 .venv/bin/python -m playwright install chromium` after `pip install -e '.[dev]'` (+ `install-deps chromium` on bare hosts). Operational requirement (OPERATIONS §10 row), not a defect. | OPERATIONS §10 | S4 | RECORDED | — |
| F-R182I-04 | Sandbox `/tmp` is a 493 MB tmpfs; a fresh-clone gate with its own venv there hit `No space left on device`. Gate clones live under `/home/user/gates/` (disk). Sandbox condition, not a repository finding. | first gate attempt on `26b98684` | S4 | RECORDED | — |
| F-R182I-05 | M2 browser proof timed out in the interactive sandbox shell while passing under `env -i`: `python3 -m apps.main` inherited the shell's provider keys (`GSK_API_KEY`, `OPENAI_API_KEY`, …) and composed REAL providers, so the execute turn from a test left the hermetic `local_echo` path. A test that can reach a live provider depending on the operator's shell is not hermetic. | `pytest tests/ui/test_command_center_browser_r182.py` in the sandbox shell → `TimeoutError` at `wait_for_function` (frames never reached `final`); `env -i …` → passed | S2 (hermeticity; potential live spend from a test) | FIXED in the test only: the proof server's env strips the provider-key names the canonical suite strips (`env -u`, OPERATIONS §10) plus `DATABASE_URL`/`REDIS_URL`. No production change. | R182-IMPL (closed) |

Formally out of scope (recorded, not worked): F-R182-01 (stale `tables.py` doc; frozen tree) → R183; Q7 / Provider slice → R183; App Factory contract definition → operator.
