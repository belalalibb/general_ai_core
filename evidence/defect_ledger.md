# R167-A — Defect Ledger

Round: R167-A v2 (OFFLINE-ENVELOPE — see `evidence/credentials_manifest.md`).
Sources: `evidence/live_closure.md` (§7), `evidence/provider_routing_matrix.md` (§4B),
`evidence/credential_binding_boundary.md` (§4C), `evidence/error_classification_map.md` (§4D),
`evidence/stage2/stage2_transcript.log` + `stage2_findings.json` (§12/§10, 50 verdict lines, 82 requests),
`evidence/stage2/s17_admin_ab_after_fix.log` (§17; the pre-fix §17 run was lost to sandbox reset #8 — its
D-09 observation is reproduced in `evidence/fixes/D-09.md` from the captured shell output and re-derived by the fail-first test).
Server profile under test: in-memory, `identity_mode=hybrid`, ADMIN_EMAILS set, single real provider (genspark_llm, in-band refusal on every call).

Severity: S1 = tenant/credential boundary broken or silent billable loss; S2 = wrong outcome booked / must-audit or gate missing on a contracted path;
S3 = honesty/observability gap, no data crosses a boundary; S4 = cosmetic.
Class: MISBOOK (wrong settlement), MISCLASS (wrong error category), ABSENT (contracted capability not present), POSTURE (composition default),
SILENT (accepted-and-ignored input), AUDIT-GAP (must-audit event never emitted), ORDER (gate ordering).

| ID | Area | Sev | Repro command | Observed | Expected | Class | Status | Commit |
|---|---|---|---|---|---|---|---|---|
| D-01 | Execution settlement | S2 | `evidence/tasks/r167a_stage1/01_simple.log`; `GET /v1/executions/f1a345c5…` (`evidence/live_closure.md` LC-1) | genspark_llm HTTP 200 whose body is a plan-refusal sentence is booked SUCCEEDED, settled 1.0 unit; parent reasoning fails `invalid_proposal`; `/diagnosis` says "no provider error was recorded". Reproduced this round on every §12 execute (45+ runs, all "succeeded" with refusal content, all billed) | In-band refusal classified (CONTENT_REJECTED / credential-indicting), run FAILED, 0 units | MISBOOK | OPEN — HANDOFF R168 (needs adapter-level refusal detection; no such contract exists, §4A item 3) | — |
| D-02 | Groq normaliser | S2 | `pytest tests/certification -k shape` (`MAP\|…` rows); `evidence/error_classification_map.md` | FastAPI-style `{"detail": …}` 400 with no `error` object → `bad_request`, request-indicting ⇒ no failover | Unknown 400 shape → non-indicting category so the route can fail over | MISCLASS | OPEN — HANDOFF R168 | — |
| D-03 | Multi-account routing | S3 | `pytest tests/certification -k two_credentials_same_provider` | Second credential for the same provider key raises `DuplicateRegistration`; `AccountPoolManager`/`ResourceSelector` have no call sites; `CandidateScore.account_id` always None | Class A (same provider, different account) failover | ABSENT | OPEN — HANDOFF R168 (design in `credential_binding_boundary.md` HANDOFF) | — |
| D-04 | Audit / attribution | S3 | `grep -rn "PROVIDER_ACCOUNT_USED\|CREDENTIAL_CREATED" apps core --include=*.py` → contracts only | No emitter names the credential/account used by a call; `modality_costs.provider_usage` carries node_key+usage only | Per-call audit row with credential logical id; cost attributable to credential | AUDIT-GAP | OPEN — HANDOFF R168 | — |
| D-05 | Credential containment (responses) | — | `probe_stage2.py::p0_credential_containment` → `= P0-4 admin views`, `= P0-4 error surfaces` | GSK_API_KEY value absent from `/v1/admin/{system,providers,audit,models}`, failure bodies, trace, diagnosis, events | absent | — | CLOSED — HELD [MEASURED] | 2afcbcf |
| D-06 | Credential containment (logs) | — | `= P0-4 server log` (server stdout scanned after ~45 requests) | GSK_API_KEY value absent from server stdout/stderr | absent | — | CLOSED — HELD [MEASURED] | 2afcbcf |
| D-07 | Auth lifecycle (in-memory profile) | S2 | `curl -s http://127.0.0.1:8000/v1/usage` (no Authorization) → 200; `curl -s -X POST …/v1/execute -d '{"ask":"…"}'` → 200 succeeded, 1.0 unit charged to the demo tenant (`stage2_transcript.log` `= P2-1 anon read`, `= P2-1 anon execute`) | Anonymous caller is silently mapped to the composition-owned demo principal (hybrid mode, `apps/composition/runtime.py` L805 unconditional in in-memory profile; `apps/api/app.py` L590-596); no env knob disables the fallback | 401 `unauthenticated` for a missing bearer whenever auth is composed, or an explicit opt-in knob | POSTURE | FIXED (R168) — demo principal is an explicit dev opt-in `DEV_DEMO_PRINCIPAL=1` (closed by default); `PUBLIC_PATHS` + admission middleware before body validation; route-enumeration test `tests/composition/test_d07_tokenless_401.py` (79 paths, 0 leaks); `evidence/r168/D-07/` | see R168 report |
| D-08 | Authz under composition | S3 | `curl -X POST …/v1/execute -H "authorization: Bearer <dev>" -d '{"ask":"Reply OK.","project_id":"<ops project id>"}'` → 200 (`= P0-2 project_id`) | `project_id` (and any non-UUID string, e.g. `not-a-uuid`) is accepted and ignored — no consumer of `ExecuteRequest.project_id` in apps/ or core/ (`grep -rn "\.project_id"` → only workspaces.py); execution is NOT attached to the foreign project; nothing crosses tenants | Named denial (404 byte-identical to unknown) or attachment to a caller-owned project; never silent acceptance of a foreign reference | SILENT | OPEN — HANDOFF R168 | — |
| D-09 | Admin control-plane audit | S2 | pre-fix (captured shell output, file lost to reset #8; see `evidence/fixes/D-09.md`): `POST /v1/admin/engineering/grants {"tenant_id":"<dev>","permissions":["workspace.exec"]}` → 200; `GET /v1/admin/audit` total unchanged | An admin mutates ANOTHER tenant's firewall policy (adds write permissions) with zero audit row; `SECURITY_POLICY_CHANGED` had no emitter anywhere; `EngineeringAdminSurface.grant` had no audit port | Every admin mutation lands in audit with actor, target tenant, permissions, outcome (§17; 20 §9 must-audit) | AUDIT-GAP | FIXED — fail-first `evidence/fixes/D-09_fail_first.txt` (exit 1) → `D-09_after_fix.txt` (103 passed, exit 0); live `evidence/stage2/s17_admin_ab_after_fix.log`: `security_policy_changed` row with actor_id/actor_tenant_id/permissions/outcome + SECURITY notification observed | 2ce3aba |
| D-10 | Admin gate ordering | S3 | `for p in …; curl -X POST …/v1/admin/<p> -H "authorization: Bearer <dev>" -d '{}'` → 422 on 16/64 routes (`= §17 non-admin denied before validation`) | FastAPI validates the typed body BEFORE `_admit(request)` on POST routes; a NON-ADMIN receives field-level schema hints (e.g. `Input should be 'fs.write', 'cmd.run', 'git.commit', 'git.push' or 'git.merge'`); valid bodies are then 403 (`= §17 valid-body mutation by non-admin` HELD) — no admission, but the admin schema is enumerable | 403 first regardless of body on every `/v1/admin/*` route | ORDER | FIXED (R168) — same admission middleware: 401 anonymous / 403 non-admin on all 59 admin operations BEFORE validation, no route-signature change; `tests/composition/test_d10_admin_gate_order.py` (14→0 typed-body leaks); `evidence/r168/D-10/` | see R168 report |
| D-11 | Audit coverage | S3 | After ~45 executions, 30+ denials (403/404/422) and admin reads by two tenants: `GET /v1/admin/audit` → types `['login']` only (`= §10 audit coverage`) | `PERMISSION_DENIED`, `CROSS_TENANT_ACCESS_DENIED`, `CREDENTIAL_CREATED/REVOKED`, `PROVIDER_ACCOUNT_USED` have zero emitters; the audit read is reader-tenant-scoped, so an admin sees only their own tenant's rows (the D-09 row for a foreign target is written but not readable by the acting admin over HTTP) | Denials and cross-tenant probes leave audit rows; platform admin can read them | AUDIT-GAP | OPEN — HANDOFF R168 | — |

## §12 / §10 probes that HELD (45 of 50 verdicts, all [MEASURED])

Tenant isolation: foreign execution by-id/events/trace/diagnosis → 404; list excludes foreign rows; workspace/project/webhook read+delete by foreign tenant → 404 and object survives; project into foreign workspace → 404 "Unknown workspace id"; conversation_id reuse by another tenant yields no history leak. Authz composition: unknown tools → 422 named. Credential containment D-05/D-06. Atomicity: failed explicit-model run (503) charged 0.0. Idempotency: same key → same execution id, 1.0 unit across two calls; key is tenant-scoped (different tenant → distinct execution). Attribution: ops units unchanged while dev executes. Pressure: 12 concurrent → 12×200, 12 distinct ids, ledger delta 12.0 = successes. Auth: malformed bearer 401; logout revokes (401 after). Input: empty/wrong-type/200k/unknown-policy → 422, malformed JSON → 422, no 500 anywhere. §10: dev on `/v1/admin/capabilities` → 403; admin tool via agent path → 422 named; anon on admin → 403; every audit row carries actor_id+tenant_id.

## §17 observations recorded, not booked as defects

- Admin is **platform-global** (`is_admin = email in ADMIN_EMAILS`); "App A admin vs App B admin" scoping is **NOT SUPPORTED BY CURRENT CONTRACT**. Within that contract the tenant-scoped views held [MEASURED]: admin B cannot list, revoke (`{"revoked":false,"reason":"authorization unknown"}`), or read audit for admin A's tenant; A's ticket survived B's revoke attempt; B cannot read A's execution (404).
- `GET /v1/admin/plans/{tenant}` and `POST /v1/admin/engineering/grants` operate on any tenant id — designed platform-admin power; the gap was auditing (D-09, fixed), not admission.
- `GET /v1/admin/executions/{id}/evaluations` returns `{"evaluations": []}` 200 for foreign and non-existent ids alike — nothing leaks; absent and foreign are indistinguishable.
- `GET /v1/admin/usage` returns `{"usage": []}` for an admin whose own tenant has no executions — it is tenant-scoped, not a platform view [OBSERVED].

## Summary

S1: 0 found / 0 fixed. S2: 4 found (D-01, D-02, D-07, D-09) / 1 fixed (D-09). S3: 5 (D-03, D-04, D-08, D-10, D-11) / 0 fixed. S4: 0.
Containment probes D-05/D-06: HELD. Accepted changes used: 1/5 (FIX D-09, +36/−5 LOC).
