# R179 state ledger (one row per item; self-contained: command, observed number, one-line meaning)

Contract: R179 OPERATOR DIRECTIVE + CONFIRMATION (Part A spec, Part B authority). Baseline `main` f5cbe48c.
Branch `genspark_ai_developer_r179`. Mode developmental. Budget: declared ceiling 6 production files
(elastic 8 if 4.5 triggers, absolute 9 with a design note here BEFORE the commit). docs/ and engineering/
are outside the counted roots. Recovery mechanism: the ONLY approved one — remote branch head is the
durable checkpoint; each sandbox reset = `git switch -C genspark_ai_developer_r179 origin/genspark_ai_developer_r179`,
rebuild `.venv` and workspace PostgreSQL binaries. Credential: sandbox credential store only, never tracked.

| item | files | budget | preflight | command | observed | verdict |
|---|---|---|---|---|---|---|
| R0 recovery | — | 0/6 | HEAD==origin/main f5cbe48c, tree clean, branch published | `git switch -C genspark_ai_developer_r179 origin/main; git push -u` | remote head f5cbe48c | READY |
| B7 budget declared | engineering/verification/green_manifest.json | 0/6 | round_r179 absent | additive JSON edit: `round_r179` (ceiling 6, log []) + `pytest.gate.min_passed` 3127 → 3504 (4.9 new floor) | `git diff`: 13 insertions, 1 deletion (the floor line only) | DECLARED |
| 4.6-a alembic rehearsal | tests_live/r179/{run_local_postgres.sh,test_deploy_truth_postgres.py} | 0/6 | fresh DB, no alembic_version, 0 tables | `bash tests_live/r179/run_local_postgres.sh -k alembic` (REAL `python -m alembic upgrade head` / `downgrade base` / `downgrade -1` as subprocess; pgvector shipped into the workspace cluster for 0007) | 2 passed: base→0020 (all metadata tables present, `vector` extension created) →base (0 tables) →0020 again; populated 0020 downgrade REFUSED (rc≠0, row kept), empty downgrade → 0019 | VERIFIED / LIVE |
| R0-b reset #1 | — | 0/6 | uncommitted runner+test lost at reset; recreated byte-equivalent, committed BEFORE running | b37167c7 → b7bd85b9 | RECOVERED |
| 4.4 durability measured | tests_live/r179/test_durability_measured_postgres.py (probe only) | 0/6 | fresh real-alembic schema; `python -m apps.main` child; SIGKILL −9 between 3 phases | `bash tests_live/r179/run_local_postgres.sh -k four_pillars` | P1 memory_blocks 1→0, preferences 1→0 (DIES); GOLD arm blocked (promote 409, F-R179-02); P2 conversation path 500/500/500 (F-R179-01); P3 audit 1→0→0 (DIES); P4 usage used 5.0→2.0 (RESETS). Raw: `evidence/r179/durability_measured.json`. Findings F-R179-01..04. | MEASURED — 4.5 TRIGGERED for P1 (composition seam); P3/P4 = DEC-B deferral made visible |
| R0-c resets #2–#5 | — | 0/6 | each reset lost only uncommitted work; ledger rows re-entered from the observed run | recovery per approved mechanism | remote head 61acc214 | RECOVERED |
| 4.5 design note (BEFORE first production commit) | apps/composition/memory.py (NEW), apps/composition/runtime.py (durable branch binding only) | 0/6 → 2/6 planned (ceiling 8 because 4.5 triggered) | P1 memory + P2 conversations composed as `InMemoryMemoryStore()` / `InMemoryConversationStore()` in BOTH profiles although `DatabaseBindings.memory` / `.conversations` (migration 0007 / 0002 tables) already exist and are never consumed | smallest composition-seam repair: adapt the EXISTING Postgres repositories onto `MemoryStorePort` / `ConversationStorePort` via `AsyncBridge.run` (verbatim `apps/composition/workspaces.py` pattern), bind them ONLY when `DATABASE_URL` is set; in-memory profile byte-identical; no route/schema/migration/dependency change; F-R179-01 (FK to `conversations` vs in-memory store) closes by the same seam | tests-first hermetic `tests/composition/test_durable_memory_r179.py`; re-measure with `-k four_pillars` → after-JSON | PLANNED |
| 4.5 correction landed | apps/composition/memory.py (new), apps/composition/runtime.py | 2/6 (ceiling 8 since 4.5 triggered) | hermetic test `TestRuntimeBinding` failed first (no import/binding) | adapters + durable-branch binding; manifest `round_r179.log` 2 entries; ruff/mypy clean | `pytest tests/composition/test_durable_memory_r179.py tests/composition/test_runtime_pb.py tests/api/test_preferences_r177.py tests/composition/test_repo_map_r177.py tests/composition/test_learning_custody_r178.py` → all passed (1 skipped: live) | LANDED — re-measure pending |
| 4.5 re-measured (AFTER) | evidence/r179/durability_measured.json (after, head 63625443) vs durability_measured_before.json (before, head 6f29873f) | 2/6 | same probe, same SIGKILL protocol, fresh real-alembic schema | `bash tests_live/r179/run_local_postgres.sh -k four_pillars` | P1 memory_blocks 1→1 (was 1→0), preference item identical id/last_seen after restart, custody row VALIDATED; P2 execute+conversation_id 200/200 and 200 after restart (was 500/500/500), blocks_total 2→2 (was 2→1); P3 audit 1→0→0 UNCHANGED; P4 usage 5.0→2.0 UNCHANGED (DEC-B deferred, visible) | 1 passed — F-R179-01 CLOSED, F-R179-03 CLOSED, F-R179-04 OPEN (deferred by design), F-R179-02 OPEN (decision queue) |
| 4.6-b rolling pair + 4.6-c crash during write | tests_live/r179/test_deploy_truth_rolling_crash_postgres.py (probe only) | 2/6 | fresh DB; old writer worktree `.venv/old_writer_2c197229` (recreated on demand by the test); 0019 → register tenant (old binary) → stop → 0020 → restart BOTH binaries | `bash tests_live/r179/run_local_postgres.sh -k "rolling_pair or crash_during"` | 4.6-b: hold rows 1; new writer under hold 404; OLD writer under hold **201** (F-R179-05, NOT refused by schema); SIGKILL old; release `released:true`; new writer 201; orphans 0/0. 4.6-c: capture blocked inside its transaction (pg_sleep trigger on `learning_samples`, `write_in_flight_when_killed` 1), SIGKILL −9, client `RemoteProtocolError`, rows after crash 0/0 (zero orphans), restart + SAME idempotency key → 201, repeat → 201 same id, final 1/1, orphans 0/0 | 4.6-c PASSED; 4.6-b measured (first run failed the naive "refused" assertion — converted into a recorded finding, evidence kept) |
| 4.2 self-description | apps/api/capabilities.py, apps/api/app.py (+ tests/api/test_capability_catalog_r177.py pin 22→23 with justification, tests/api/test_capability_shelf_coverage_r179.py) | 4/6 (ceiling 8) | `/v1/admin/learning/custody/*` mounted by create_admin_router with NO shelf row; hermetic profile mounts 82 paths | id `learning.custody_governance` (ONE authorized widening); row derived from the mount condition (admin+memory+custody+audit) | pytest: catalog r177 + v7 + self_review + admin_agent (167 passed); shelf coverage (4 passed): AVAILABLE iff routes mounted, INERT for each weaker composition, every mounted `/v1/admin` family owned by a specific row (wildcard row excluded), every mounted family in the shipped profile has an AVAILABLE row | LANDED |
| 4.3 action discovery | apps/api/capabilities.py (2nd edit, same slot), apps/api/admin.py (new route) + tests/api/test_action_discovery_r179.py | 5/6 (distinct files: memory.py, runtime.py, capabilities.py, app.py, admin.py; ceiling 8) | no consumer could learn which admin actions exist except by reading `AdminAction`/UI hand-list | `admin_actions_json()` derived from `ACTION_AREA` + `FINAL_ACTIVE_ADMIN_AREAS`; `GET /v1/admin/capabilities/actions` | 5 passed: pure function of vocabulary, one owner per action, inactive areas empty, no enum literals in producer, admin-gated route, shelf row shape `{id,state,evidence}` untouched; admin_api + catalog v7 + shelf coverage green | LANDED — payload-schema half deferred (DEC-A) |
| 4.7 design note (BEFORE commit — absolute cap 9) | apps/api/intake.py (slot 6), infrastructure/db/learning.py (7), apps/composition/learning.py (8), core/learning/lifecycle.py (9); apps/api/admin.py already counted | 5/6 → 9/9 planned (ceiling 8 elastic since 4.5 triggered; absolute 9 permitted only with this note) | (a) no tenant-scoped read of `learning_policy_revocations` (holds/revocations) exists — an operator cannot see WHY capture answers 404; (b) `release-legacy-hold` answers `released:false` for BOTH "never held" and "already released" and never refuses an invariant break; (c) `POST /learning/intake` loses every landed row when a later row's custody write raises (404/409/500 for the whole batch) and cannot tell "row refused" from "durable layer unavailable" | (a) `GET /v1/admin/learning/custody/holds` — repository `list_revocations` (NO reconciled-assert: a held tenant must see its hold) → adapter → `LearningLifecycleService.custody_holds` → route; (b) route computes `outcome ∈ {released, already_released, no_hold}` from holds-before + audit history, re-reads holds-after and REFUSES (409, LearningError) when custody's answer contradicts the durable state; (c) `IntakeAdapter.ingest` catches `LearningStorageConflict`/`LearningStorageError` per row as refused rows (content-free reasons) and treats any other exception as a LAYER fault: stops, reports landed + refused + not_attempted rows, route answers 503 with the report in `details` | tests-first `tests/api/test_operator_visibility_r179.py`; conscious pin update of the exact release JSON in `tests/api/test_external_evidence_p01_r178.py` (adds `outcome`) | PLANNED |
| 4.7 operator visibility LANDED | infrastructure/db/learning.py, apps/composition/learning.py, core/learning/lifecycle.py, apps/api/intake.py (+ admin.py, app.py same slots) | 9/9 (absolute cap; design note committed first at 60d09d95; production commit ccfa9a7d) | tests-first `tests/api/test_operator_visibility_r179.py` (7) failed on 404/old shape | (a) `GET /v1/admin/learning/custody/holds`; (b) `outcome` tri-state + 409 on contradiction; (c) per-row refusal vs layer fault (503 + partial report) | conscious pin updates in `test_external_evidence_p01_r178.py` (release JSON gains `outcome`; per-row refusal → 201 report instead of whole-batch 404; double models the hold row) | LANDED — verification run recorded in the next row |
| 4.7 verification | tests/api/test_operator_visibility_r179.py (7), tests/api/test_external_evidence_p01_r178.py, tests_live/r178 (59) | 9/9 | — | ruff clean; mypy clean on the 6 touched modules; hermetic suites (operator visibility, external evidence, intake, custody, learning, shelf coverage, action discovery, admin_agent, ui) all passed | `bash tests_live/r178/run_local_postgres.sh` → 59 passed after ONE conscious live pin update: a DB fault mid-intake (trigger RAISE on row 2) is now a 503 retryable response carrying the partial report (row 1 landed, rows 2–3 not_attempted) instead of a re-raised DBAPIError | VERIFIED |
| FINAL GATE | committed clone 8b9875ea, `env -i`, canonical `check_repo.sh` | 9/9 | — | `evidence/r179/final_gate_8b9875ea.txt` (identical numbers on d747b158: `final_gate_d747b158.txt`) | **3533 passed / 0 failed / 0 errors / 64 skipped** (floor 3504, ceiling 64); mypy --strict, ruff, import-linter, secret scan (5/5), no .env, change budget (round_r179=9/9), not_evaluated=2; gateway `gateway_final.txt` **194 passed**; live r178 **59**, r179 **5**; frozen trees `ui/ apps/admin_agent/ core/tools/gate.py PROJECT_EXECUTION_STATE.md final_docs_v3` zero diff vs f5cbe48c (60_DECISION_LOG.md append-only lines only) | RESULT: PASS |
| RULINGS Q1–Q5 received | docs/ai_orchestration_pack/R179_HANDOFF.md §B | 9/9 (round_r179 closed at its cap; rulings open `round_r179_rulings`) | Q1 APPROVED (durable audit+usage, 4.5 seam pattern; screens wait); Q2 RULED: structural guard at the database, NO trigger/policy logic in DDL, no Python boot check; Q3 RESOLVED BY CAPABILITY: promote-to-GOLD completable over HTTP via `GraderType.SECURITY` activation (graders.py:44 designed path), real 201 with non-vacuous security check + real 409 on failure; Q4 APPROVED: ONE declared source of payload field rules read by validator AND shelf; Q5 THAW = next round's opening commit (N0=73 arithmetic binding). Token: not a blocker; rotation is operator's action after merge; handoff item #2 deleted; §D credential-free. Merge: merge COMMIT (no squash); gate re-run on the merge commit. | order chosen: Q1 → Q3 → Q4 → Q2; each tests-first, logged in manifest BEFORE production commit | RECORDED |
| RULINGS budget declared | engineering/verification/green_manifest.json `round_r179_rulings` | 0/8 | rulings Q1–Q4 need production changes; round_r179 is closed at 9/9 | new round key, ceiling 8, log [] | additive JSON edit only | DECLARED |
| Q1 design note (BEFORE commit) | apps/composition/audit_usage.py (new), apps/composition/runtime.py | 0/8 → 2/8 planned | **Audit**: `DatabaseBindings.audit` (`audit_events`, FK tenants only) never consumed — same seam as 4.5. **Usage**: `usage_ledger.execution_id` is NOT NULL FK → `executions.id` (RESTRICT, UNIQUE); (i) `ExecutionService.execute_*` calls `usage.reserve(execution_id)` BEFORE the `executions` row exists on sync/agent paths (`apps/api/app.py` puts the report AFTER the service returns) → FK violation; (ii) `core/tools/executor.py:166` reserves under a `call_id` that never has an `executions` row → FK violation on billed tool calls. Neither is a composition seam. | plan: (1) bind durable AUDIT, tests-first, re-measure P3; (2) bind durable USAGE behind the same adapter and MEASURE live — record FK refusals as F-R179-06, never mask; (3) raise Q6 to the operator | PLANNED |
| Q1 LANDED (hermetic) | apps/composition/audit_usage.py (new), apps/composition/runtime.py | 2/8 | tests-first `tests/composition/test_durable_audit_usage_r179.py` (9) failed on missing module/binding | adapters + hoisted durable decision (bridge/bindings built once, before the execution service); in-memory profile unchanged | composition + admin_api + admin_agent suites: 407 passed, 4 skipped; ruff clean; mypy clean (runtime.py, audit_usage.py) with ONE recorded ignore (F-R179-07) | LANDED — live measurement (P3/P4 re-run, expected usage FK refusals) next |
| Q1 live MEASURED at be407d2e (audit+usage bound) | evidence/r179/F06_usage_ledger_fk_violation.txt | 2/8 | fresh real-alembic schema, `python -m apps.main` child, SIGKILL protocol | `bash tests_live/r179/run_local_postgres.sh -k four_pillars` | FAILED: every `POST /v1/execute` 500 — `ForeignKeyViolationError` on `usage_ledger.fk_usage_ledger_execution_id_executions` (reserve before the executions row; tool calls never have one). Recorded as **F-R179-06**, not masked. | MEASURED — Q1 usage half blocked by schema/ordering |
| Q1 FIX landed | apps/composition/runtime.py (same slot, 2/8 unchanged) | 2/8 | binding durable usage breaks the product | durable AUDIT stays bound; durable usage adapter composed then consciously NOT bound (`del durable_usage`; one-name flip after Q6); `usage: InMemoryUsageAccounting` in both profiles removes the F-R179-07 type: ignore; test pins updated (`InMemoryUsageAccounting()` count 2, `F-R179-06` cited at the decision point) | 385 passed / 4 skipped (composition + admin_agent); ruff + mypy clean; commit 5e522d45 | LANDED |
| Q1 re-measured (AFTER) | evidence/r179/durability_measured_after_q1.json (head 5e522d45) | 2/8 | same probe, same SIGKILL protocol | `bash tests_live/r179/run_local_postgres.sh -k four_pillars` | **P3 audit 1→1→1 (was 1→0→0) — STABLE across restart**; P1 memory 1→1, P2 conversation 200/200 unchanged; **P4 usage 5.0→2.0 UNCHANGED** (F-R179-06 → Q6). OPERATIONS §13 rewritten to the measured truth; HANDOFF §B gains Q6, §C rows for F-R179-04 (audit closed / usage open), -06, -07 | 1 passed — audit half of DEC-B MET; usage half decision-gated |
