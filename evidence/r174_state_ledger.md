# R174 state ledger (checkpoint discipline: one row appended BEFORE each item; recoverable from this file alone)

Question: are external providers wired correctly into the core? Proof: AssemblyAI LLM gateway, real key,
through the WHOLE chain (gateway-service provider → canonical contract → RemoteGatewayAdapter → core port).
Hard condition: zero AssemblyAI-specific lines in core/. Deliberate check: two providers, same model name, no collision.

| item | intended change | HEAD at start |
|---|---|---|
| §0 | sandbox reset #4 recovered (venv rebuilt, creds restored, HEAD==origin/main c94217c); this ledger created | c94217c |
| §1 | READ: AssemblyAI LLM gateway reference (web) + repo external-provider reference (contract, runbook, example, template, existing real provider) → reconciliation note in evidence/r174/01_read/ | c94217c |
| §2 | Direct upstream probe (key form raw vs Bearer, error body shape, cheapest model) — 3 calls max, key only in env var, evidence with key redacted → evidence/r174/02_upstream_probe/ | 5830ed0 |
| §3 | gateway-service/providers/assemblyai/ (definition, adapter, _upstream, __init__) copied from groq's shape; hermetic tests gateway-service/tests/providers/test_assemblyai.py; ONE registration line in gateway-service/app.py (same line Groq has — F-1); zero lines in core/ apps/ providers/real/ | 732624c |
| §3′ | sandbox reset #5 wiped the untracked §3 files (ledger row survived, work did not) — rebuilt from groq's shape + §2 evidence, 57 hermetic tests, gateway suite 179/179, pushed as b131345. Lesson applied: commit+push every file the moment it exists | 3d1ede4 |
| §4 | Live E2E through the GATEWAY process only (uvicorn :8800, GW_ASSEMBLYAI_API_KEY in env, route_map token→assemblyai): one success on qwen3.5-4b-32k-fast, one unknown-model → model_unavailable; ≤3 paid calls; request/response captured key-redacted → evidence/r174/04_gateway_live/. Then F-2/F-3 probing of the platform→gateway link | b131345 |
| §4′ | sandbox reset #6 wiped local 59077a0 + gateway process + key env; commit restored byte-identical from the uploaded git bundle (fast-forward on 9c689e7), pushed. Lesson held: the bundle upload was the only thing that saved §4 | 9c689e7 |
| §5 | PLATFORM→GATEWAY link (the last link). (a) F-3 live: build the REAL runtime profile (apps.composition) with an onboarded gateway provider using `route_token_ref` and show whether the route token can resolve at all (predicted SecretNotFound); (b) F-2 live: onboard a `user_key` gateway definition through the real composition path (predicted ValueError); (c) if (a) has a legitimate seam, drive ONE paid call platform→gateway→AssemblyAI via RemoteGatewayAdapter → core port, envelope built by the platform; (d) collision check: groq + assemblyai both declaring the same model name, route by provider not by model name. Evidence → evidence/r174/05_platform_link/. Key must be re-supplied (lost in reset #6) for (c) only | 59077a0 |
| §5′ | DONE (75164dd): platform→gateway link broken in 3 places, live, two processes, 0 paid calls. F-3 confirmed (500 SecretNotFound, nothing reaches gateway); F-2 confirmed (409); F-6 NEW: gateway `/v1/health`=UNKNOWN (health_supported=false) → adapter UNAVAILABLE → walker step 6 refuses, so fixing F-3 alone does not open the door. Collision check (d) deferred: it needs one onboarding to succeed first | 07a1b24 |
| §6 | MINIMAL FIXES, one commit each, test-pinned, no core/ changes: (F-3) a real custody seam for route tokens — env `GATEWAY_ROUTE_TOKENS="ref=token,..."` loaded into `onboarding_secrets` under PLATFORM_TENANT_ID at composition time (mirrors how provider keys reach their manager at runtime.py:564; value never logged); plus onboarding route maps SecretNotFound → 409 with the ref name (not a bare 500). (F-6) the walker treats a health surface that reports UNKNOWN with `checked_at=None` as UNVERIFIED-and-recorded (same 41 §49 posture step 5 already has for a missing check surface), NOT as a refusal — implemented at the adapter boundary (adapter raises the existing NotImplementedError family / returns a distinguishable answer) so core/providers/onboarding.py stays untouched if possible. Then re-run §5 probe expecting E → 201. F-2 left recorded | 75164dd |
| §6′ | sandbox reset #7 wiped the uncommitted F-3 edits (gateway.py seam, runtime.py wiring, route 409 mapping, test file) — ledger row survived, work did not. Rebuilding F-3 from the §6 plan; every file committed+pushed the moment it exists | 3c709af |
| §6-F3 | DONE locally (4ff7b5b feat seam · bec9d37 route 409 · f5d073e+7e2579f tests, 18/18; neighbouring suites green; core/ untouched). `GATEWAY_ROUTE_TOKENS="ref=token,..."` → `PreloadedSecrets` decorator over `InMemorySecretManager` under PLATFORM_TENANT_ID; route maps `SecretNotFound` → 409 `{details:{credential_ref}}`. Hermetic replay of §5 case E: without env → 409 naming the ref (was 500); with env → walker passes custody and refuses at step-6 (the F-6 door, next). PUSH BLOCKED: GitHub credentials lost in reset #7 — bundle uploaded https://www.genspark.ai/api/files/s/URSeug9O (restore: `git fetch <bundle> HEAD && git merge --ff-only FETCH_HEAD`) | c12e4f1 |
| §6-F3′ | sandbox reset #8 wiped the sandbox again (fresh clone at 3c709af, bundle file gone from /tmp). Restored c12e4f1..b1a3d0f byte-identical from the uploaded bundle (fast-forward), pushed with a user-supplied token → origin/main == b1a3d0f. F-3 tests 18/18 re-verified post-restore. Third time the bundle upload was the only surviving copy. Next: F-6 | b1a3d0f |
| §6-F6 | DONE (7d49d2d adapter · abd10b5 walker · 5ead7d8+a0a7efb tests). Adapter boundary: `/v1/health` → `UNKNOWN` **with `checked_at=null`** (the gateway's contract answer for `health_supported=false`, i.e. no check ran) now raises `GatewayHealthCheckUnsupported(NotImplementedError)` — a missing SURFACE, not a verdict; a CHECKED UNKNOWN still maps to UNAVAILABLE ("unknown is never healthy" unchanged). Walker step 6 catches `NotImplementedError` exactly as step 5 does → recorded UNVERIFIED, walk continues; any returned non-HEALTHY state still refuses. core/ change = 11 lines in onboarding.py, mirroring existing step-5 posture. §5 case E replayed hermetically through `build_runtime_profile` + real admin door against a loopback gateway stub answering the live §5-H shape: **201**, provider DISABLED/not routable, steps 5+6 listed UNVERIFIED, `gw_alpha/stub-model-1` registered, route token seen only in `X-Route-Token`, never in the response; checked-UNKNOWN variant still 409 at step 6. Full suite 3156 passed / 58 skipped / 2 xfail / 0 failed. Next: §7 live re-run of §5-E against the real gateway process (needs key), then collision check (d) | a0a7efb |
| §7′ | sandbox reset #9 wiped the venv, the gateway process, the key env, and the UNTRACKED §7 run-1 artifacts (`evidence/r174/07_live_chain/{K_onboard_groq_same_model_name,L_execute_groq_same_name,checks}.json` — run 1 of the collision check, being reclassified as misdesigned when the reset hit: it onboarded groq with the same model name but groq has no key at the gateway, so the second leg could only ever answer `platform_credential_missing`, proving nothing about routing). Tracked state intact: HEAD==origin/main c39e7f1. Rebuilding venv; §7 restarts with the corrected design (below), every artifact committed the moment it exists | c39e7f1 |
| §7 | LIVE CHAIN + COLLISION, corrected design. (a) start real gateway (uvicorn :8800) with `GW_ASSEMBLYAI_API_KEY` + route_map; start real platform (`build_runtime_profile`) with `GATEWAY_ROUTE_TOKENS`; onboard assemblyai via the admin door → expect 201 (F-3+F-6 live); ONE paid call platform→gateway→AssemblyAI via RemoteGatewayAdapter → core port. (b) collision: a second gateway provider slug that declares the SAME model name as assemblyai but is a hermetic FIXTURE (`gateway-service/providers/fixture_echo/`, no upstream, no key, returns a recognisable marker) — onboard both, execute against each, assert the marker proves routing went by PROVIDER not by model name; 0 extra paid calls. Zero lines in core/ | c39e7f1 |
| §7-run2 | DONE, 0 paid calls (0b89332 fixture_echo · 76e7b87 probe · 3d1c5c8 evidence). `gateway-service/providers/fixture_echo/` — hermetic slug declaring EXACTLY AssemblyAI's `qwen3.5-4b-32k-fast`, answers with a fixed MARKER, no upstream/key; opt-in only (`GW_ENABLE_FIXTURE_ECHO=1`), 11 hermetic tests, gateway suite 192/192. LIVE two-process run (`evidence/r174/07_live_chain/run2_free/`, 16/16): G both route tokens resolve to different providers with the same model name · E1 assemblyai onboarded **201** through the real admin door (F-3+F-6 now live, gateway log shows /v1/health + /v1/models from the platform) · E2 fixture_echo onboarded 201 with the same name → distinct model_keys `assemblyai/…` vs `fixture_echo/…`, no collision · N both enabled via draft→validate→preview→publish · **X1 execute pinned to `fixture_echo/<model>` → 200 with the FIXTURE MARKER** (routed by PROVIDER, not by name; POST /v1/execute seen at the gateway) · X3 `assemblyai/<model>` pinned to provider fixture_echo → 503 model_unavailable "excluded by explicit provider selection", nothing reached the gateway · X2 paid leg SKIPPED honestly (key absent after reset #9; never simulated). Secrets scrubbed + asserted absent. core/ apps/ providers/ diff vs c39e7f1 = 0 lines. Platform suite exit 0, 0 failures. Reset #10 wiped the tree mid-handoff; restored from bundle PaMbDTai (c39e7f1..3d1c5c8), no loss. REMAINING: §7-X2 = the ONE paid call platform→gateway→AssemblyAI — rerun `GW_ASSEMBLYAI_API_KEY=… python3 evidence/r174/07_live_chain/probe.py` when the key is re-supplied (driver puts it in the gateway env only) | 3d1c5c8 |
| §7-run3 | DONE — THE ONE PAID CALL (b780639 evidence). Reset #11 hit first: fresh clone at c39e7f1, venv gone, 5 commits gone from the object store; restored c39e7f1..f6d4c78 byte-identical from bundle 79ZGysud (ff, verified), pushed with a user-supplied token → origin/main == f6d4c78 (fifth time the bundle was the only surviving copy). Venv rebuilt. Then `GW_ASSEMBLYAI_API_KEY=… python3 evidence/r174/07_live_chain/probe.py` → `evidence/r174/07_live_chain/run3_paid/`, **18/18, paid_calls=1**: every run2 check re-passed live (G, E1 201, E2 201 same name → distinct keys, N enabled ×2, X1 fixture MARKER, X3 cross-pin 503 with nothing at the gateway) PLUS **X2: execute pinned to `assemblyai/qwen3.5-4b-32k-fast` → 200, content `chain verified`, no marker, usage settled 1 unit, POST /v1/execute seen at the gateway** — i.e. platform admin door → registry → RemoteGatewayAdapter → gateway-service assemblyai slug → AssemblyAI upstream, real key, whole chain. Key lived only in the driver/gateway env; evidence scrubbed + 32-hex shape scan clean + grep for the key across run3_paid = 0 hits. Hard condition re-verified: `grep -ri assemblyai core/` = 0 files; core/ touched across ALL of R174 (c94217c→HEAD) = 1 file, onboarding.py, +16/−7, the F-6 step-6 NotImplementedError posture only. Gateway suite 192/192 (exit 0), platform suite via `apps.cli test` exit 0. R174 question answered YES with evidence; F-2 (`user_key` gateway definitions → 409) remains the one recorded, unfixed finding | b780639 |
