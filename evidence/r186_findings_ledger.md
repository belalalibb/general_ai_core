# R186 findings ledger (every measurement that FAILED or contradicted the brief; a finding gets an entry, never an off-books fix)

Scope of the round: F-R185-L01 and F-R185-L02 only (carried in from `evidence/r185_findings_ledger.md`). Anything else discovered is RECORDED here and NOT fixed under `round_r186`.

| id | finding | evidence | severity | disposition | owning round |
|---|---|---|---|---|---|
| F-R185-L01 (carried) | 390 px horizontal overflow after a FAILED execution (unwrapped `error` frame JSON) | `evidence/r185/live_preview_e2e/overflow_390_root_cause.txt` | medium | **FIXED** (`command.css`: `overflow-wrap: anywhere` on `.frame`/`.error`) — browser proof: recorded live frame 511 px → 0 px overflow at 390 px (`evidence/r186/browser_proof_r186.json`) | R186 |
| F-R185-L02 (carried) | literal `undefined` for `verification.*` when `verification` is null (`reasoning_failed` / `invalid_proposal`) | `evidence/r185/live_preview_e2e/converse_verification_undefined.txt` | low | **FIXED** (`command.js`: null `verification` rendered by name citing `stop_reason`; five served counters verbatim otherwise) — browser proof: 5× `undefined` → none (`evidence/r186/browser_proof_r186.json`) | R186 |

Nothing new raised during R186 (the ruff E501 gate failure on 96fa0dd6 was a lint slip in the new test, fixed in 44fc99b6 — ledger row 4).
| F-R185-L03 (carried, re-verified on main 1028212c) | Groq organization restricted — key recognized (altered key → 401 `invalid_api_key`; supplied key → 400 `organization_restricted`); QEVION maps it to `invalid_credential` / 502 and records the failure honestly | `evidence/r186/live_preview/{execute_1.json,groq_direct_models_probe.json,server_groq_error_line.txt}` | blocker for provider-backed SUCCESS (external) | NOT FIXABLE FROM THE REPOSITORY — operator: key from an unrestricted Groq organization, or Groq support | operator |

**Proof semantics note (closure reconciliation, 2026-09-16 — `evidence/r186/proof_semantics_L01_L02.txt`):** for F-R185-L01 and F-R185-L02 two distinct claims are on record and must not be conflated. (A) **Proof of rendering** — `tests/ui/test_command_center_fix_r186.py` replays the RECORDED live Groq `error` frame and an injected `verification: null` body through Playwright `page.route` (network-layer substitution): 511 px → 0 px overflow, `undefined` → 0; deterministic, repeatable, the regression guard of record. (B) **Proof of natural live occurrence** — once, on the merged build `1028212c` (`evidence/r186/live_preview_e2e/preview_e2e.json`): the runtime itself produced `reasoning_failed` / `verification: null` and a live `invalid_credential` `error` frame at 390 px, rendered with 0 px overflow and 0 × `undefined`. (B) is a single observed confirmation, not a frequency claim; the SUCCESS path of a live Groq execution remains BLOCKED (F-R185-L03). Disposition of L01/L02: FIXED and VERIFIED (A + B).
