# R200 state ledger — Command capability → surface routing + tenant centre (UI-only; ceiling 0)

| # | step | sha / ref | evidence | result |
|---|------|-----------|----------|--------|
| 1 | opening declaration (before code) | branch `genspark_ai_developer_r200` from `main f6991e16` | `round_r200` ceiling 0; R200-DEC-01 (operator D1–D5 verbatim); `R200_HANDOFF.md` OPEN; `R200_POINTER` | DECLARED |
| 2 | RED | bb7946fb → pin correction f4380cdb | `evidence/r200/red.txt` | 14 FAILED / 8 passed on the opening tree. Pin corrected before GREEN: the evidence route is parsed by an escaped regex, never a quoted `/v1/` literal (12 frozen). NOTE: four sandbox resets during this round erased unpushed work (implementation once, pin fixes twice, evidence once); redone from the pushed checkpoint each time — nothing unpushed reached the branch. |
| 3 | GREEN (R200-A/B/C) | 5953f2b7 (impl) → 2744fde0 (guard 21/21) | `evidence/r200/green.txt`; `evidence/r200/browser_probe.py.txt` + `browser_probe_2744fde0.json`; `command_tenant.png`, `command_admin.png`, `admin_deeplink_catalog.png` | Static UI + composition pins 200/200 GREEN. Real browser: **tenant request set = {/healthz, /v1/auth/login, /v1/auth/session, /v1/executions}** (zero `/v1/admin/*`, zero `/v1/agent/*`); **admin request set = R185 set** {/healthz, admin/capabilities, admin/system, admin/usage, auth/login, auth/session, executions}. Tenant tier: 4 Workbench links + one locked admin node `aria-disabled=true` quoting `session.is_admin = false`. Admin tier: 24 nodes classified — 16 linked (href = served mount + hash), 3 inert/unavailable "not linked: node is <state>", 5 routeless "no surface owns this route". Receivers: `/app/#view=models` → Models active; `/admin/#surface=catalog` → catalog rail active. |
| 4 | invariants at 2744fde0 | working tree | this row | command.js `/v1/` 12 + one `fetch(` in api(); ui/app/app.js 22 / 4; ui/admin/app.js 73; no `/v1/` in css; no href with `/v1/`; `CAPABILITY_IDS` not quoted (R182 guard GREEN). |
