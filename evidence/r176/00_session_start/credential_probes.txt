# Credential liveness probes at session start (status codes only; values never recorded)
# GitHub token from prompt §0.6 / handoff:
#   REST GET /repos/belalalibb/general_ai_core  -> 401
#   git ls-remote origin main                    -> OK (public read, unauthenticated)
#   git push --dry-run origin main               -> "Invalid username or token" => PUSH BLOCKED
# GROQ_API_KEY value committed at prompt line 133:
#   GET https://api.groq.com/openai/v1/models    -> 400 (organization_restricted class; same as R175 F-R175-03 keys)
# GW_ASSEMBLYAI_API_KEY value committed at prompt line 134:
#   GET https://api.assemblyai.com/v2/transcript?limit=1 -> 200  => LIVE CREDENTIAL IN TRACKED FILE (S1)
# Env vars GITHUB_TOKEN / GROQ_API_KEY / GW_ASSEMBLYAI_API_KEY in the sandbox process env: absent.
