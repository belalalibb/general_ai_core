# R182 findings ledger (every measurement that FAILED or contradicted the brief; a finding gets an entry, never an off-books fix)

Precedent: `evidence/r181_findings_ledger.md`. Severities: S1 release-blocking … S4 cosmetic. Round budget: `round_r182` ceiling **0** — no finding may be fixed inside `core/ apps/ infrastructure/` in R182.

| id | finding | evidence | severity | disposition | owning round |
|---|---|---|---|---|---|
| F-R182-01 | `infrastructure/db/tables.py` module documentation (lines 140-141) describes `usage_ledger.execution_id` as `UNIQUE + FK (RESTRICT: accounting records must never dangle)` while migration 0022 (R181 Q6) removed that FK. The column-level comment (lines 683-687) is correct; the module docstring is stale. Reviewer finding raised against R181; the PR #20 pre-merge window was not used (merge performed on the operator's "R181 accepted"). | `grep -n "UNIQUE + FK" infrastructure/db/tables.py` → 140; `infrastructure/db/migrations/versions/0022_usage_ledger_key.py` drops `fk_usage_ledger_execution_id_executions` | S4 (doc-only, no behaviour) | **DEFERRED — frozen tree, ceiling 0.** `infrastructure/` is frozen in R182; a docstring edit inside the counted tree is a production-tree change. Zero R182 budget effect. Never silently cleaned up. | **R183** (when `infrastructure/` is unfrozen) |
| F-R182-02 | `ui/admin/app.js` sits at `/v1/` count **73 = N0** — zero headroom. Any new admin-console route literal needs a genuine pay-down first (deleting a real call), not a string trick. Not a defect of the current console; a hard boundary for R182-IMPL. | `evidence/r182/governance_frame_743e203f.txt` | S3 (governance boundary) | RECORDED. R182-IMPL rule: the Command Center lives in a NEW tree with its OWN declared literal ceiling (R182_HANDOFF §2/§13); the `ui/admin/app.js` literal count may not rise. Manifest+baseline N0 pair edit ONLY on explicit operator authorization after impossibility of pay-down is proven. | R182-IMPL |
| F-R182-03 | `green_manifest.baseline.json` `ui.bytes` (app.js 79351 / index.html 32385) no longer matches on-disk bytes (80789 / 32456) since the R180 Q5 thaw. No test consumes `ui.bytes`; `bytes_previously_recorded` equals `bytes`. Repository data drift, not a gate failure. | `evidence/r182/governance_frame_743e203f.txt` | S4 | RECORDED as data; **NOT mutated** in R182 (prompt §6). Fix owner: whichever round next re-captures the baseline under authorization. | deferred |
| F-R182-04 | APEX provenance: `CREDITS.md` attribution links for the two 21st.dev MIT components (ShaderBackground, overview lamp panel) are unresolved placeholders (`<21st.dev component URL / author>`); `ApexOverviewPanel.tsx` carries no in-file attribution. README: "the name Apex and the Reznikov Engineering branding are not part of this license". | `evidence/r182/apex_reference_inspection.txt` | S3 (licensing/provenance) | RECORDED. Consequence for R182-IMPL: ShaderBackground and the lamp panel are **REBUILD or DISCARD, never copy**; no APEX source vendored/imported; no Apex name/branding; no ROSTER/INFO; no weather/social external behaviour. Provenance status: UNRESOLVED (upstream). | R182-IMPL (constraint) |
| F-R182-05 | Browser proof dependency: `playwright` not importable in the venv; the repository's NOT-EVALUATED item #1 is exactly "live-suite: browser automation … — missing dependency". No Playwright installed in R182 (prompt §19). | `evidence/r182/governance_frame_743e203f.txt` | S3 | RECORDED. R182-IMPL acceptance: a real browser run against the served Command Center REPLACES NOT-EVALUATED #1 (count 2→1) — never a third item, never a ceiling raise. Dependency install is an implementation-round act under its own declaration. | R182-IMPL |
| F-R182-06 | Self-inflicted gate FAIL on the R182 branch head `d0d6a8df`: `evidence/r182/governance_frame_743e203f.txt` lines 24-28 quoted the five sentinel VALUES (`AKIA…EXAMPLE`, PEM header) of the manifest secret-scan exceptions verbatim; the manifest regex scans `*.txt`, so the evidence file matched its own quotation → `FAIL: possible secret detected outside the exception list` (5 hits). Nothing real leaked (documented AWS example key + PEM header literal). | fresh-clone gate on `d0d6a8df` (superseded; see `evidence/r182/gate_head_<sha>.txt` for the corrected head) | S3 (gate integrity) | **FIXED in evidence only**: the five values redacted in place (`AKIA<redacted-documented-example-key>`, `<redacted PEM private-key header>`); manifest `secret_scan.exceptions` NOT grown (ceiling 5/5 untouched), no production tree touched, budget 0/0 unchanged. Rule for later rounds: evidence quotes the scan's FILE:LINE, never the matched VALUE. | R182 (closed) |

Formally out of scope (recorded, not worked): **Q7** (evaluation-distinction gap: `GET /v1/admin/executions/{id}/evaluations` returns `[]` for an un-evaluated run; the UI must render "not evaluated" as a distinct state, never a score) and the **Provider slice** (live account verification / selection enforcement) — both **R183 backend**.

Severity totals: S1 0 · S2 0 · S3 4 (3 recorded, 1 fixed in evidence) · S4 2 recorded · production-tree fixes 0 (ceiling 0, readiness round).
