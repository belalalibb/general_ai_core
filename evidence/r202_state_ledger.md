# R202 state ledger — Templates + App Factory UX (UI-only; production ceiling 0; app.js /v1/ 22 -> 23)

| # | step | sha / ref | evidence | result |
|---|------|-----------|----------|--------|
| 1 | opening declaration (before code) | branch `genspark_ai_developer_r202` from `main 22111694` | `round_r202` ceiling 0; `v1_count_ceiling_app_js` 22 → 23 (operator ruling, declared before code); R202-DEC-01 (operator D1–D8 verbatim); `R202_HANDOFF.md` OPEN; `R202_POINTER` | DECLARED |
| 2 | RED (guard before implementation) | `795fcd83` on opening tree `114c6307` | `evidence/r202/red.txt` — 7 FAILED (detail route not consumed; markup absent; R197 D4 pin flip); R201 `<= 23` | RED |
| 3 | GREEN + real-browser proof | `9a6f89cb` (UI tree = `3c42d641`) | `evidence/r202/green.txt` 43/43 (R202+R197+R201 guards); static UI+composition 205 passed / 0 FE (`static_suite_9a6f89cb.txt`); `browser_probe.py.txt` → `browser_probe_9a6f89cb.json`: one `GET /v1/templates/app_factory.plan@1` per choice; panel rows == served stages (kind/role/depends_on/instruction; null model_policy → "auto (Router decides)"); 0 controls in panel; empty choice → hidden, 0 requests; restored context → exactly one read; unknown ref → served `validation_error: unknown template.` verbatim; `localStorage` keys []; session context carries no token. PNGs: `workbench_template_detail.png`, `workbench_template_unknown_404.png`, `workbench_template_async_422.png`, `workbench_template_sync_200.png` | GREEN |
| 3a | **FINDING R202-F1 (recorded, not worked around)** | `9a6f89cb` | Server refuses `execution_strategy` + `execution_policy.async` with 422 (`apps/api/app.py` R188 C3 "execution_strategy runs synchronously in this slice") — rendered verbatim in `#ask-error`, 0 timeline rows. Template runs are always sync 200; the Workbench opens SSE only after a 202 ⇒ **R202-B (`stageLabel` in `followEvents`) is not reachable through the shipped UI in this slice**. API-level SSE replay of the same execution DOES carry `node_started` for all 3 stages (emitter exists; UI sync path never consumes it). R202-B kept as approved (D4 = i; 0 literals, 0 reads); proposal §B premise on async reach was wrong → ruling required at the decision gate (keep latent / remove / backend round lifting R188 C3 for templates) | RECORDED |
| 4 | invariants | `9a6f89cb` | `ui/app/app.js` `/v1/` 23 (== declared ceiling) · `fetch(` 4 · `localStorage` 0 · `app_factory` literal 0; `command.js` 12 + one `fetch(`; `ui/admin/app.js` 73; freeze `--check` MATCHES (46); production diff vs `origin/main` 0 lines (`changes_used 0`) | HOLD |
